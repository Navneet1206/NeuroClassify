# Copy this file to .env and fill in values as needed.
# Location: d:\git_projects\NeuroClassify\api_app\.env
#
# The backend supports two modes of inference:
# 1) External REST API (MODEL_API_URL) — preferred for production inference
# 2) OpenRouter (LLM-based, multimodal) — convenient for demos, subject to credits/limits
#
# If MODEL_API_URL is set, the backend will use it and ignore OpenRouter settings.

###############################################
# General server settings
###############################################
HOST=127.0.0.1
PORT=8000
TIMEOUT_SECONDS=30
# Comma-separated list of allowed origins for CORS
ALLOWED_ORIGINS=http://localhost:8501,http://127.0.0.1:8501

###############################################
# Option A: External inference API (recommended)
###############################################
# MODEL_API_URL=https://your-inference-provider/api/v1/predict
# MODEL_API_KEY=your-provider-api-key

###############################################
# Option B: OpenRouter (LLM) — for demos
###############################################
# IMPORTANT: Keep this key secret. Do NOT commit your real key.
# OPENROUTER_API_KEY=sk-or-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Default model; you can change to a cheaper or more permissive one if needed.
OPENROUTER_MODEL=google/gemini-2.5-pro
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1/chat/completions
OPENROUTER_APP_TITLE=NeuroClassify-API
OPENROUTER_SITE_URL=http://localhost
# Limit response size to reduce credits usage; 64–256 are typical values
OPENROUTER_MAX_TOKENS=128

###############################################
# Usage notes
###############################################
# - To use External API: set MODEL_API_URL (+ MODEL_API_KEY if required). Leave OPENROUTER_API_KEY unset.
# - To use OpenRouter: set OPENROUTER_API_KEY. Leave MODEL_API_URL unset.
# - Restart the backend after changing this file, so new values load.
#   From repo root (PowerShell):
#     Set-Location d:\git_projects\NeuroClassify
#     .\.venv310\Scripts\Activate.ps1
#     uvicorn api_app.backend.main:app --host 127.0.0.1 --port 8000 --reload
# - Health check: http://127.0.0.1:8000/health
# - API docs:     http://127.0.0.1:8000/docs
